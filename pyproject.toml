[project]
name = "weather-forecast"
version = "0.1.0"
description = "Consulta de previsión meteorológica semanal usando Open-Meteo API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
]
